{
  "name": "@simtrace/e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end tests for the SimTrace platform",
  "scripts": {
    "type-check": "tsc --noEmit",
    "test": "playwright test --config=../playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "typescript": "^5.7.0"
  }
}
